/**
 * Define a devmoji configuration with full type support.
 * This is an identity function that provides TypeScript autocompletion.
 *
 * @param {import('./index').DevmojiConfig} config
 * @returns {import('./index').DevmojiConfig}
 */
export function defineDevmojiConfig(config) {
  return config;
}
